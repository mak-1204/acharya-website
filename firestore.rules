rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // LIBERAL RULES: Allowing all reads and writes for development and testing
    // Last updated: 2025-05-20T12:00:00Z (Trigger redeploy)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}